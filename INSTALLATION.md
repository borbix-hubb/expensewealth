# 🚀 คู่มือติดตั้งแบบย่อ

## ขั้นตอนการติดตั้ง 5 ขั้นตอน

### 1️⃣ สร้าง Google Spreadsheet
- ไปที่ https://sheets.google.com
- สร้าง Spreadsheet ใหม่
- ตั้งชื่อตามต้องการ

### 2️⃣ เปิด Apps Script
- คลิก Extensions > Apps Script
- ลบโค้ดเดิมทั้งหมด
- คัดลอกโค้ดจากไฟล์ `Code.gs` วางลงไป
- บันทึก (Ctrl+S)

### 3️⃣ Deploy Web App
```
Deploy > New deployment
Select type: Web app
Execute as: Me
Who has access: Anyone
Deploy > คัดลอก Web App URL
```

### 4️⃣ แก้ไขไฟล์ app.js
เปิดไฟล์ `js/app.js` แล้วแทนที่:
```javascript
const SCRIPT_URL = 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE';
```
ด้วย URL ที่คัดลอกมา

### 5️⃣ เปิดใช้งาน
- เปิดไฟล์ `index.html` ด้วยเบราว์เซอร์
- เริ่มใช้งานได้เลย!

---

## 📝 ตัวอย่าง Web App URL
```
https://script.google.com/macros/s/AKfycbz.../exec
```

## ⚠️ หมายเหตุสำคัญ

1. **อนุญาตการเข้าถึง** - เมื่อ Deploy ครั้งแรก Google จะขออนุญาต ให้กด Allow
2. **Anyone Access** - ต้องเลือก "Anyone" เพื่อให้เว็บแอปเข้าถึงได้
3. **HTTPS** - URL ต้องเป็น HTTPS เท่านั้น
4. **URL ใหม่** - ทุกครั้งที่ Deploy ใหม่ จะได้ URL ใหม่ ต้องอัปเดตในไฟล์ app.js ด้วย

## 🎯 ทดสอบการทำงาน

### ทดสอบใน Apps Script
1. เปิด Apps Script Editor
2. เลือกฟังก์ชัน `testAddTransaction`
3. กดปุ่ม Run (▶️)
4. ตรวจสอบ Google Sheets จะมีข้อมูลทดสอบ

### ทดสอบเว็บแอป
1. เปิด index.html
2. ลองเพิ่มรายการ
3. กด F12 เปิด Console ดู error (ถ้ามี)
4. ตรวจสอบ Google Sheets

## 🔧 แก้ปัญหาเบื้องต้น

| ปัญหา | วิธีแก้ |
|-------|---------|
| ไม่สามารถบันทึกได้ | ตรวจสอบ URL ในไฟล์ app.js |
| ไม่แสดงข้อมูล | กดปุ่ม "รีเฟรช" หรือตรวจสอบ Console |
| Permission denied | Deploy ใหม่และเลือก "Anyone" |
| Script URL ไม่ถูกต้อง | คัดลอก URL จาก Deploy อีกครั้ง |

## 📞 ต้องการความช่วยเหลือเพิ่มเติม

อ่านคู่มือฉบับเต็มได้ที่ `README.md`

---

**เวลาติดตั้งโดยประมาณ: 5-10 นาที**
