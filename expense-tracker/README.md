# 💰 เว็บแอปบันทึกรายรับรายจ่าย

เว็บแอปพลิเคชันสำหรับบันทึกรายรับรายจ่ายที่เชื่อมต่อกับ Google Sheets ผ่าน Google Apps Script ใช้งานง่าย สวยงาม และใช้ฟอนต์ Kanit

## ✨ ฟีเจอร์

- 📊 แสดงสรุปยอดรายรับ รายจ่าย และยอดคงเหลือ
- ➕ เพิ่มรายการรายรับ-รายจ่ายพร้อมหมวดหมู่
- 🔍 กรองข้อมูลตามเดือนและประเภท
- 📝 ดูประวัติรายการย้อนหลัง
- 🗑️ ลบรายการ
- 📱 Responsive Design รองรับทุกหน้าจอ
- 🎨 UI สวยงามด้วยฟอนต์ Kanit
- ☁️ เก็บข้อมูลบน Google Sheets (ฟรี)

## 📁 โครงสร้างไฟล์

```
expense-tracker/
├── index.html          # หน้าเว็บหลัก
├── css/
│   └── style.css      # ไฟล์ CSS
├── js/
│   └── app.js         # JavaScript สำหรับ frontend
├── Code.gs            # Google Apps Script (ต้องอัปโหลดไปยัง Google Apps Script)
└── README.md          # คู่มือนี้
```

## 🚀 วิธีติดตั้ง

### ขั้นตอนที่ 1: สร้าง Google Spreadsheet

1. ไปที่ [Google Sheets](https://sheets.google.com)
2. สร้าง Spreadsheet ใหม่
3. ตั้งชื่อ เช่น "บัญชีรายรับรายจ่าย"
4. จดบันทึก Spreadsheet ID จาก URL
   ```
   https://docs.google.com/spreadsheets/d/[SPREADSHEET_ID]/edit
   ```

### ขั้นตอนที่ 2: ตั้งค่า Google Apps Script

1. ใน Google Sheets ให้คลิกที่เมนู **Extensions** > **Apps Script**
2. ลบโค้ดเริ่มต้นทั้งหมด
3. คัดลอกโค้ดจากไฟล์ `Code.gs` และวางลงไป
4. (ตัวเลือก) แก้ไข `SPREADSHEET_ID` ในโค้ดหากต้องการระบุ Spreadsheet เฉพาะ
   ```javascript
   const SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID_HERE';
   ```
   หรือปล่อยว่างเพื่อใช้ Spreadsheet ที่เปิดอยู่
5. บันทึกโปรเจค (กด Ctrl+S หรือ Cmd+S)
6. ตั้งชื่อโปรเจค เช่น "Expense Tracker API"

### ขั้นตอนที่ 3: Deploy Web App

1. คลิกปุ่ม **Deploy** > **New deployment**
2. คลิกไอคอน ⚙️ (เฟือง) ข้างๆ "Select type"
3. เลือก **Web app**
4. ตั้งค่าดังนี้:
   - **Description**: Expense Tracker Web App
   - **Execute as**: Me
   - **Who has access**: Anyone
5. คลิก **Deploy**
6. อนุญาตการเข้าถึง (Authorize access)
   - คลิก **Review permissions**
   - เลือกบัญชี Google ของคุณ
   - คลิก **Advanced** > **Go to [Project Name] (unsafe)**
   - คลิก **Allow**
7. คัดลอก **Web app URL** ที่ได้

### ขั้นตอนที่ 4: ตั้งค่าเว็บแอป

1. เปิดไฟล์ `js/app.js`
2. แทนที่ URL ในบรรทัดที่ 3:
   ```javascript
   const SCRIPT_URL = 'YOUR_GOOGLE_APPS_SCRIPT_WEB_APP_URL_HERE';
   ```
   เป็น Web App URL ที่คัดลอกมาจากขั้นตอนที่ 3

### ขั้นตอนที่ 5: เปิดใช้งาน

1. เปิดไฟล์ `index.html` ด้วยเบราว์เซอร์
2. เริ่มใช้งานได้เลย!

## 💡 วิธีใช้งาน

### เพิ่มรายการ

1. เลือกประเภท: รายจ่าย หรือ รายรับ
2. กรอกจำนวนเงิน
3. เลือกวันที่
4. เลือกหมวดหมู่
5. กรอกรายละเอียด (ถ้ามี)
6. กดปุ่ม "บันทึก"

### กรองข้อมูล

1. เลือกเดือนที่ต้องการดู
2. เลือกประเภท (ทั้งหมด, รายรับ, รายจ่าย)
3. กดปุ่ม "กรอง"
4. กด "รีเซ็ต" เพื่อแสดงข้อมูลทั้งหมด

### ลบรายการ

1. คลิกไอคอนถังขยะ 🗑️ ที่รายการที่ต้องการลบ
2. ยืนยันการลบ

## 🎨 หมวดหมู่

### รายจ่าย
- อาหาร/เครื่องดื่ม
- ค่าเดินทาง
- ค่าที่พัก
- ช้อปปิ้ง
- บันเทิง
- สุขภาพ
- การศึกษา
- ค่าน้ำค่าไฟ
- ค่าโทรศัพท์/อินเทอร์เน็ต
- อื่นๆ

### รายรับ
- เงินเดือน
- โบนัส
- ขายของ
- ดอกเบี้ย
- เงินลงทุน
- อื่นๆ

## 🔧 การแก้ไขและปรับแต่ง

### เพิ่มหมวดหมู่

แก้ไขไฟล์ `js/app.js` ที่ตัวแปร `CATEGORIES`:

```javascript
const CATEGORIES = {
    expense: [
        'หมวดหมู่ใหม่',
        // ... หมวดหมู่อื่นๆ
    ],
    income: [
        'หมวดหมู่ใหม่',
        // ... หมวดหมู่อื่นๆ
    ]
};
```

### เปลี่ยนสี Theme

แก้ไขไฟล์ `css/style.css` ที่ส่วน `:root`:

```css
:root {
    --primary-color: #6366f1;  /* สีหลัก */
    --success-color: #10b981;  /* สีรายรับ */
    --danger-color: #ef4444;   /* สีรายจ่าย */
    /* ... */
}
```

### ใช้ Spreadsheet เฉพาะ

แก้ไขไฟล์ `Code.gs`:

```javascript
const SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID';
```

## 📊 โครงสร้างข้อมูลใน Google Sheets

Sheet จะถูกสร้างอัตโนมัติชื่อ "Transactions" พร้อมคอลัมน์:

| ID | วันที่ | ประเภท | หมวดหมู่ | รายละเอียด | จำนวนเงิน | เวลาบันทึก |
|----|--------|--------|----------|------------|-----------|------------|
| 1 | 2025-01-15 | รายจ่าย | อาหาร | อาหารเช้า | 150.00 | 15/1/2568, 08:30:00 |

## 🐛 การแก้ปัญหา

### ไม่สามารถบันทึกข้อมูลได้

1. ตรวจสอบว่าใส่ Web App URL ถูกต้องในไฟล์ `app.js`
2. ตรวจสอบว่า Deploy Web App แล้วและเลือก "Anyone" ในส่วน "Who has access"
3. ลอง Deploy ใหม่และใช้ URL ใหม่

### ไม่แสดงข้อมูล

1. ตรวจสอบ Console ในเบราว์เซอร์ (กด F12)
2. ตรวจสอบว่ามีข้อมูลใน Google Sheets หรือไม่
3. ลองกดปุ่ม "รีเฟรช"

### ข้อผิดพลาด CORS

- Google Apps Script ใช้ mode `no-cors` ซึ่งเป็นปกติ
- การบันทึกจะทำงานแม้จะไม่สามารถอ่าน response ได้

## 📱 การ Deploy บนเว็บ

### GitHub Pages

1. สร้าง repository ใหม่บน GitHub
2. อัปโหลดไฟล์ทั้งหมด (ยกเว้น `Code.gs`)
3. ไปที่ Settings > Pages
4. เลือก Branch: main
5. กด Save
6. เข้าถึงได้ที่ `https://[username].github.io/[repo-name]`

### Netlify

1. ไปที่ [netlify.com](https://www.netlify.com)
2. ลากโฟลเดอร์ `expense-tracker` ไปวาง
3. รอการ deploy เสร็จ
4. ได้ URL ทันที

## 🔒 ความปลอดภัย

- Web App URL เป็น public แต่ข้อมูลอยู่ใน Google Sheets ของคุณเท่านั้น
- ไม่มีการเก็บข้อมูลส่วนตัวอื่นใด
- ใช้ HTTPS สำหรับความปลอดภัย

## 📄 License

MIT License - ใช้งานได้ฟรี สำหรับโปรเจคส่วนตัวและเชิงพาณิชย์

## 💪 พัฒนาเพิ่มเติม

### ฟีเจอร์ที่อาจเพิ่มในอนาคต

- 📈 กราฟสรุปรายรับรายจ่ายรายเดือน
- 📊 รายงานสรุปรายปี
- 💾 Export ข้อมูลเป็น PDF/Excel
- 🔔 แจ้งเตือนเมื่อใกล้เกินงบประมาณ
- 👤 ระบบ Login หลายผู้ใช้
- 💵 รองรับหลายสกุลเงิน
- 🏷️ ระบบแท็ก
- 🔄 ซิงค์ข้อมูลอัตโนมัติ

## 🙏 ขอบคุณ

- Google Fonts สำหรับฟอนต์ Kanit
- Google Apps Script สำหรับ Backend ฟรี
- Google Sheets สำหรับ Database ฟรี

---

สร้างด้วย ❤️ โดยใช้ Google Apps Script และ Web Technologies
